---
"@myworldx/eslint-config": minor
---

Add a `./next-js` export for Next.js applications, layering
@next/eslint-plugin-next's recommended and core-web-vitals rules over the React
configuration. `apps/www` is its first consumer.
